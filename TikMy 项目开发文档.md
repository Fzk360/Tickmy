# TikMy 项目开发文档

## 1. 项目概述

### 1.1 项目名称
`TikMy` 本地生活视频收藏与浏览工具

### 1.2 项目定位
TikMy 是一款基于 `uni-app + Vue3` 开发的本地优先视频收藏与浏览小程序，面向本地生活内容收藏场景，强调“数据主权归用户、离线可用、沉浸浏览、精准归类”。

用户可以将本地生活相关短视频一键从相册导入到小程序中，在本地设备内完成存储、分类、浏览、互动与推荐，不依赖云端服务器即可完成核心功能。

### 1.3 产品目标
- 实现完全本地优先的数据管理体验
- 支持离线状态下完整运行
- 提供类似短视频平台的沉浸式刷片体验
- 支持仓库化管理，方便用户对视频精准归类
- 基于本地互动行为完成智能推荐
- 保持与现有 `vue-ui` 原型在视觉与交互上的高度一致

---

## 2. 一期范围定义

### 2.1 一期目标端
一期优先交付 `微信小程序`。

### 2.2 一期核心能力
- 从相册批量导入视频
- 本地文件保存与本地元数据持久化
- 自定义仓库创建与管理
- 沉浸式上下刷视频
- 点赞、收藏、评论、弹幕
- 倍速播放、双击放大
- 基于本地行为的推荐排序
- 个人页与设置页
- 完整离线可用

### 2.3 一期不包含
- 云端存储
- 用户账号体系
- 多设备同步
- 分享到外部平台
- 服务端推荐系统
- 公共社交评论体系
- 搜索与复杂标签系统

---

## 3. 产品原则

### 3.1 数据主权
所有核心数据优先保存在用户本地设备，不默认上传到任何云端。

### 3.2 离线优先
无网络时，导入后的视频播放、仓库浏览、评论弹幕、推荐排序等功能必须完整可用。

### 3.3 原型还原
以现有 `vue-ui` 为唯一视觉和交互基线，小程序端做到“视觉和交互尽量 1:1”，如遇平台限制则采用等效替代实现，不改变信息架构与核心体验。

### 3.4 可扩展架构
一期虽然不使用 SQLite，但数据访问层必须预留适配接口，方便未来 App 端切换到 SQLite。

---

## 4. 技术方案

### 4.1 技术栈
- 框架：`uni-app`
- 前端：`Vue 3`
- 样式：`SCSS`
- 运行端：`微信小程序`
- 本地存储：小程序本地存储 + 小程序本地文件系统
- 后续可扩展数据库：`SQLite（App 端优先）`

### 4.2 存储策略
由于一期目标端为微信小程序，第一版不使用 SQLite，采用如下本地优先策略：

- 视频文件：保存到小程序本地文件区
- 封面图：本地生成或提取后保存
- 元数据：保存到小程序本地存储
- 互动数据：本地存储
- 评论与弹幕：仅保存在当前设备本地

### 4.3 数据层抽象要求
业务层不得直接操作 `uni.setStorageSync` 或零散本地存储 API，必须统一通过数据适配层访问。

适配层一期实现：
- `MiniProgramStorageAdapter`

后续扩展预留：
- `SQLiteAdapter`

统一接口约定：
- `loadAll()`
- `saveVideo()`
- `saveVideos()`
- `saveRepository()`
- `saveComment()`
- `saveDanmaku()`
- `saveInteraction()`
- `reset()`
- `exportableSnapshot()`

---

## 5. 页面与模块设计

### 5.1 首页刷片页
首页是沉浸式视频浏览主入口，采用竖向切换刷片体验。

#### 功能要求
- 自动播放当前视频
- 上下滑动切换视频
- 离开当前视频时自动暂停
- 展示作者、标题、描述、所属仓库
- 支持点赞、收藏、评论入口
- 支持倍速播放
- 支持弹幕开关
- 支持双击放大画面
- 推荐流默认按本地推荐分排序

#### 实现约束
- 小程序端使用竖向 `swiper` 替代 Web 原型中的 scroll-snap
- 保证切换流畅，避免连续播放时明显卡顿

### 5.2 仓库页
仓库页用于集中管理视频分类。

#### 功能要求
- 展示所有仓库卡片
- 支持创建新仓库
- 支持从仓库页发起导入
- 点击仓库进入详情页
- 展示仓库封面、名称、视频数

### 5.3 仓库详情页
仓库详情页用于查看某个仓库下的视频列表。

#### 功能要求
- 展示当前仓库下全部视频
- 支持进入视频编辑
- 支持从详情页继续导入
- 支持修改视频标题、描述、所属仓库等元信息

#### 数据规则
- 一期规定一个视频只属于一个主仓库
- 不支持一个视频归属多个仓库

### 5.4 导入弹层
导入弹层用于从相册批量导入视频。

#### 功能要求
- 调用 `uni.chooseMedia` 选择视频
- 支持一次选择多个视频
- 导入时指定目标仓库
- 支持导入时新建仓库
- 检测重复文件
- 处理导入失败、容量不足、格式不支持等异常状态

#### 一期规模目标
- 单次导入规模以 `10-30 条短视频` 为主

### 5.5 评论弹层
评论为本地私有评论，仅当前设备可见。

#### 功能要求
- 查看视频评论列表
- 新增评论
- 关闭后再次打开仍保留
- 离线状态可正常读写
- 评论仅本机私有，不上传

### 5.6 弹幕系统
弹幕为本地私有弹幕。

#### 功能要求
- 按视频维度保存
- 支持开关显示
- 支持设置显示时间点
- 支持基础颜色字段
- 不做联网同步

### 5.7 个人页
个人页用于展示本地互动聚合结果。

#### 功能要求
- 展示点赞视频列表
- 展示收藏视频列表
- 显示本地个人资料区域
- 提供设置入口

### 5.8 设置页
设置页用于体现“本地优先”与存储管理。

#### 功能要求
- 显示本地优先说明
- 显示当前存储占用估算
- 提供重置本地数据入口
- 保留版本信息展示

---

## 6. 数据模型设计

### 6.1 VideoAsset
```ts
{
  id: string
  title: string
  localPath: string
  coverPath: string
  duration: number
  width: number
  height: number
  repositoryId: string
  importedAt: number
  updatedAt: number
}
```

### 6.2 Repository
```ts
{
  id: string
  name: string
  coverPath: string
  videoCount: number
  createdAt: number
}
```

### 6.3 InteractionState
```ts
{
  videoId: string
  dwellSeconds: number
  playCount: number
  playFinishCount: number
  isLiked: boolean
  isFavorited: boolean
  lastViewedAt: number
}
```

### 6.4 Comment
```ts
{
  id: string
  videoId: string
  content: string
  createdAt: number
}
```

### 6.5 Danmaku
```ts
{
  id: string
  videoId: string
  content: string
  color: string
  timePoint: number
  createdAt: number
}
```

### 6.6 AppSettings
```ts
{
  showDanmaku: boolean
  defaultSpeed: number
  storageMode: string
}
```

---

## 7. 推荐系统设计

### 7.1 推荐范围
推荐系统一期为纯本地规则推荐，不引入云端算法和模型服务。

### 7.2 输入信号
- 停留时长
- 完播次数
- 播放次数
- 点赞行为
- 收藏行为
- 最近浏览时间
- 快速划走行为

### 7.3 评分规则
推荐分公式：

```ts
score = dwellRatio * 0.4 + finishRate * 0.25 + like * 0.2 + favorite * 0.15
```

### 7.4 排序策略
- 默认按推荐分降序排序
- 冷启动阶段随机打散
- 最近快速划走的视频做降权
- 已长期高频观看内容避免过度重复曝光，可追加轻量去重策略

---

## 8. UI 还原要求

### 8.1 还原标准
以 `vue-ui` 为基准，确保以下内容高度一致：
- 页面结构
- 信息层级
- 视觉氛围
- 按钮布局
- 弹层形式
- 刷片交互
- 评论抽屉体验
- 仓库页卡片表现
- 个人页和设置页结构

### 8.2 允许的替代实现
以下部分允许做平台等效替代：
- Web `scroll-snap` 改为小程序 `swiper`
- Web 视频手势与小程序事件机制差异
- Tailwind 样式改写为 `uni-app + SCSS`
- Lucide 图标替换为本地图标资源或兼容方案

### 8.3 文案规范
现有 `vue-ui` 中存在乱码文案，迁移时统一修正为 UTF-8 中文文本，形成正式产品文案。

---

## 9. 目录与架构建议

### 9.1 页面层
建议最少包含以下页面：
- 首页刷片页
- 仓库列表页
- 仓库详情页
- 个人页
- 设置页

### 9.2 组件层
建议按业务能力拆分组件：
- `feed`
- `video-action-panel`
- `comment-drawer`
- `danmaku-layer`
- `repo-card`
- `import-sheet`
- `settings-panel`

### 9.3 分层原则
- 页面层负责组装业务
- 组件层负责 UI 与交互
- store/状态层负责页面共享状态
- adapter 层负责本地数据访问
- utils 层负责通用工具能力

---

## 10. 开发阶段划分

### 10.1 第一阶段：项目骨架与原型迁移
目标：
- 完成 `uni-app + Vue3` 页面骨架
- 建立底部导航与页面路由
- 将 `vue-ui` 核心视觉结构迁移到小程序端
- 修正文案乱码问题

交付结果：
- 首页、仓库页、个人页、设置页具备静态 UI
- 主要交互骨架可运行

### 10.2 第二阶段：本地数据层与导入能力
目标：
- 实现本地存储适配层
- 打通视频导入、本地文件保存、元数据落库
- 实现仓库创建与归类

交付结果：
- 可导入视频并离线持久保存
- 可查看仓库及仓库详情

### 10.3 第三阶段：刷片播放与互动系统
目标：
- 实现视频自动播放、切换暂停、倍速、放大
- 接入点赞、收藏、评论、弹幕
- 实现互动数据持久化

交付结果：
- 刷片核心体验闭环
- 评论与弹幕本地可用

### 10.4 第四阶段：推荐系统与设置管理
目标：
- 实现本地推荐排序
- 实现个人页互动聚合
- 实现设置页存储管理与数据重置

交付结果：
- 推荐流可根据用户行为动态变化
- 本地优先能力表达完整

### 10.5 第五阶段：性能优化与验收
目标：
- 优化连续播放和切换体验
- 验证离线能力
- 完成基础稳定性测试

交付结果：
- 满足一期发布标准

---

## 11. 测试与验收标准

### 11.1 功能验收
- 首次启动可正常进入首页、仓库页、个人页
- 可成功导入 `10-30` 条视频
- 关闭小程序再次打开后视频和仓库数据仍存在
- 可创建仓库并在导入时选择归类
- 可点赞、收藏、评论、发送弹幕
- 评论与弹幕关闭重进后仍存在
- 倍速与双击放大可正常工作
- 设置页可重置全部本地数据

### 11.2 离线验收
在飞行模式下验证以下能力：
- 视频可播放
- 仓库可浏览
- 评论可查看与新增
- 弹幕可显示与新增
- 推荐排序仍可更新
- 设置页仍可访问

### 11.3 性能验收
- 刷片切换无明显白屏
- 连续切换不卡死
- 中低端设备可基本流畅运行
- 导入后列表与首页刷新及时

---

## 12. 风险与约束

### 12.1 平台约束
微信小程序不适合作为 SQLite 一期落地端，因此本阶段必须采用本地文件 + 本地存储方案。

### 12.2 存储容量风险
当用户长期积累大量视频时，小程序本地容量可能成为瓶颈，一期通过控制导入规模与提示机制规避风险。

### 12.3 播放性能风险
多视频连续切换和高频操作可能带来性能压力，需要在播放器实例管理、预加载和资源释放上重点优化。

### 12.4 UI 差异风险
Web 原型与小程序原生能力存在差异，必须优先保证“体验等价”，而不是机械照搬实现细节。

---

## 13. 后续扩展方向

- App 端接入 SQLite
- 数据导出/备份能力
- 标签体系与多仓库归属
- 搜索与筛选
- 本地智能标签提取
- 多端同步
- 云端账号体系
- 更复杂的离线推荐模型

---

## 14. 当前默认决策

- 一期目标端：微信小程序
- UI 还原标准：视觉和交互尽量 1:1
- 数据模式：本地优先，不上云
- 评论范围：仅本机私有
- 推荐策略：本地规则推荐
- 导入规模：10-30 条短视频为一期主要使用场景
- 视频归类规则：一个视频仅归属一个主仓库
- 数据库策略：一期不使用 SQLite，仅预留 App 端扩展接口

---

## 15. 结论

TikMy 一期将围绕“本地优先、离线可用、沉浸刷片、仓库化收藏”四个核心方向建设，在微信小程序端先完成产品闭环，并通过数据适配层为未来 App + SQLite 扩展预留演进空间。

本项目开发时，所有实现必须同时满足以下三条基线：
- 不破坏本地优先原则
- 不偏离 `vue-ui` 的核心视觉与交互体验
- 不因无网络而损失核心功能
