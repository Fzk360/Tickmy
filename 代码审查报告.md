# TikMy 项目代码审查报告

审查时间：2026-04-01

审查范围：
- `pages/`
- `components/`
- `store/`
- `adapters/`
- `utils/`
- `data/`
- `pages.json`

结论概览：
- 发现 5 个需要优先处理的代码问题，其中 2 个属于较高风险的数据/交互缺陷。
- 发现 2 个明显的工程维护风险：备份文件和构建产物混入源码、缺少自动化测试。

## 1. [高] 导入按钮只是“看起来禁用”，实际上仍可重复触发导入

位置：
- `components/import-sheet.vue:18`
- `store/tikmy-store.js:528`
- `pages/repo/index.vue:98`
- `pages/repo/detail.vue:129`

问题说明：
- 导入弹窗里的按钮仅通过 `import-button--disabled` 改变样式，没有真正加 `disabled`，点击时仍然会直接 `$emit('import')`。
- `store.importVideos()` 内部虽然会把 `state.importing` 设为 `true`，但没有在函数入口做并发保护。
- 这意味着用户在导入开始后仍可能重复点击，或者在弱网/卡顿场景下触发多次导入流程，出现重复弹窗、重复 toast、并发状态竞争等问题。

影响：
- UI 与真实行为不一致。
- 导入流程可能被重复触发，增加重复数据或异常状态的概率。

建议：
- 在组件层真正禁用按钮：增加 `:disabled="importing || !selectedRepoId"`。
- 在 `store.importVideos()` 开头增加并发保护，例如检测 `state.importing` 后直接返回或抛错。

## 2. [高] 本地文件保存失败时静默退回临时路径，重启后资源可能失效

位置：
- `store/tikmy-store.js:192`
- `store/tikmy-store.js:511`
- `store/tikmy-store.js:528`

问题说明：
- `saveTempFile()` 会先调用 `uni.saveFile`，但一旦失败就直接 `return tempFilePath`。
- 这个返回值随后会被用于：
  - 头像/背景图选择 `pickProfileImage()`
  - 视频导入 `importVideos()`
- 小程序/移动端的 `tempFilePath` 通常不具备长期持久性，应用重启后很可能失效。

影响：
- 用户以为“已导入/已保存”的视频、头像、背景图，重启后可能无法访问。
- 这与项目“本地优先、离线持久化”的定位直接冲突。

建议：
- `saveFile` 失败时不要静默回退成成功路径。
- 应区分“持久化成功”和“仅临时可用”两种状态，并给出明确提示。
- 对导入视频和资料图片分别补充失败处理与回滚逻辑。

## 3. [中] 推荐排序的最近活跃 tie-breaker 方向写反了

位置：
- `store/tikmy-store.js:618`
- `store/tikmy-store.js:627`

问题说明：
- `recommendedVideos` 在分数相同时，使用下面的逻辑比较最近活跃时间：

```js
const activityDiff = Number(leftInteraction.lastViewedAt || 0) - Number(rightInteraction.lastViewedAt || 0)
if (activityDiff !== 0) {
  return activityDiff
}
```

- 这个写法会让更早被观看的视频排在更前面，而不是更近的。
- 如果设计目标是“在相同推荐分下优先展示最近仍有互动的视频”，这里的排序方向应反过来。

影响：
- 推荐流在分数接近时会更偏向旧内容，削弱“最近兴趣”信号。

建议：
- 改为按 `right - left` 排序，确保更近互动的视频优先。

## 4. [中] `saveVideos()` 会打乱视频顺序，刷新后列表顺序与当前会话不一致

位置：
- `adapters/mini-program-storage.js:43`
- `adapters/mini-program-storage.js:52`

问题说明：
- `saveVideos(videos)` 先基于旧快照构建 `map`，再用 `Object.values(map)` 回写数组。
- 对已存在 id 的重新赋值不会改变对象键的插入顺序，因此当前会话中排在前面的新导入视频，持久化后重新读取时可能被放到旧数据后面。

影响：
- 仓库详情页或其它依赖原始数组顺序的视图，在重启/重新进入后顺序会变化。
- 用户会感知到“刚导入的视频一开始在前面，之后又跑到后面”的不一致行为。

建议：
- `saveVideos()` 直接按传入数组顺序完整覆盖 `snapshot.videos`，或显式按传入数组重建结果数组。

## 5. [中] 仓库详情页的导入弹窗会直接改写当前仓库上下文

位置：
- `pages/repo/detail.vue:35`
- `pages/repo/detail.vue:38`
- `pages/repo/detail.vue:86`
- `pages/repo/detail.vue:129`

问题说明：
- 仓库详情页把 `repositoryId` 同时用于：
  - 当前详情页展示的仓库
  - 导入弹窗选中的目标仓库
- 用户只是在导入弹窗里切换目标仓库，`handleRepoSelect()` 就会立刻把页面主体切换到另一个仓库。

影响：
- 页面上下文被意外修改，用户容易迷失。
- “我只是想导入到别的仓库”会变成“详情页直接跳到别的仓库内容”。

建议：
- 将“页面当前仓库 id”和“弹窗选中的导入目标仓库 id”拆成两个独立状态。

## 6. [低] 仓库中混入了大量备份文件和构建产物，源码真相不清晰

位置：
- 备份/分支文件共 22 个，例如：
  - `components/feed-card.phase3.vue`
  - `components/edit-video-popup.fixed.vue`
  - `pages/home/index.next.vue`
  - `store/tikmy-store.clean.js`
- 构建产物位于 `unpackage/dist/dev/`，当前共有 77 个文件。

问题说明：
- 当前仓库同时存在正式源码、`next/phase3/clean/fixed/ascii` 版本文件，以及整套 `unpackage` 构建输出。
- 这会显著提高误改文件、合并冲突、评审噪音和“到底哪个才是线上版本”的沟通成本。

影响：
- 新人接手成本高。
- 代码评审和问题定位效率低。
- 容易出现“改了但没生效”或“修的是备份文件”的情况。

建议：
- 明确单一源码入口。
- 将备份文件迁出仓库或放入单独归档目录。
- 将 `unpackage/dist` 加入忽略规则，除非项目流程明确要求提交构建产物。

## 7. [低] 项目当前没有自动化测试兜底

检查结果：
- 未发现 `test` / `spec` / `__tests__` 等测试文件。

问题说明：
- 当前项目包含推荐排序、导入去重、持久化、播放行为、互动统计等较多状态逻辑，但缺少自动化验证。

影响：
- 上述排序、导入、持久化类问题很难在重构或继续迭代时被及时发现。

建议：
- 至少补充以下 3 类测试：
  - `store/tikmy-store.js` 的单元测试：推荐排序、互动统计、导入汇总。
  - `adapters/mini-program-storage.js` 的持久化测试：顺序、覆盖、重置。
  - 关键页面流程测试：创建仓库、导入视频、编辑资料、重置数据。

## 优先级建议

建议先处理顺序：
1. 导入按钮并发触发问题
2. 临时文件回退导致的持久化失效问题
3. 推荐排序 tie-breaker 方向问题
4. `saveVideos()` 顺序丢失问题
5. 仓库详情页状态耦合问题

## 备注

本次审查以静态代码阅读为主，未实际连接真机/小程序运行环境验证，因此部分问题属于“高概率行为缺陷”而非运行时复现记录。
